<!DOCTYPE html>
<html lang="ko">
  <head>
    <%- include('head'); -%>
    <title>태스크 참여 신청</title>
  </head>
  <body>
    <div class="container">
      <div class="row mt-4">
        <h2>참여 가능한 태스크 리스트</h2>
        <hr class="mb-3" />
        <div class="table-responsive-sm"></div>
        <table class="table table-hover table-striped table-sm table-bordered">
          <colgroup>
            <col width="60" />
            <col width="200" />
            <col width="100" />
          </colgroup>
          <thead class="thead-dark">
            <tr>
              <th>번호</th>
              <th>태스크 이름</th>
              <th>태스크 참여</th>
            </tr>
          </thead>
          <tbody>
            <% for(i = 0; i < list.length; i++) { %>
            <tr>
              <td><%=i+1 %></td>
              <td><%=list[i].NAME %></td>
              <td>
                <form action="/submitter/privacy_consent" method="post">
                  <input
                    type="hidden"
                    name="taskname_request"
                    value="<%=list[i].NAME %>"
                  />
                  <input type="submit" value="참여하기" />
                </form>
              </td>
            </tr>
            <% } %>
          </tbody>
        </table>
      </div>
      <hr class="mb-3" />
      <div><a href="/"> 홈으로 돌아가기</a></div>
    </div>
  </body>
</html>
