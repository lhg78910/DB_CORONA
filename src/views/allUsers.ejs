<!DOCTYPE html>
<html lang="ko">
  <head>
    <%- include('head'); -%>
    <title>All submitter List</title>
  </head>
  <body>
    <div class="container">
      <div class="row mt-4">
        <h3><a href="/"> 홈으로 돌아가기 </a></h3>
      </div>
      <div class="row mt-3">
        <h2>모든 회원 명단</h2>
        <table class="table table-hover table-striped table-sm table-bordered">
          <thead class="thead-dark">
            <tr>
              <th>ID</th>
              <th>이름</th>
              <th>성별</th>
              <th>주소</th>
              <th>생일</th>
              <th>전화번호</th>
              <th>나이</th>
              <th>유형(역할)</th>
            </tr>
          </thead>
          <tbody>
            <% for(i = 0; i < submitter.length; i++) { %>
            <tr>
              <td><%=submitter[i].ID %></td>
              <td><%=submitter[i].NAME %></td>
              <td><%=submitter[i].GENDER %></td>
              <td><%=submitter[i].ADDRESS %></td>
              <td><%=submitter[i].BIRTHDATE.toString().substring(4, 15) %></td>
              <td><%=submitter[i].PNUM %></td>
              <td><%=submitter[i].AGE %></td>
              <td><%=submitter[i].ROLE %></td>
            </tr>
            <% } %>
          </tbody>
          <tbody>
            <% for(i = 0; i < estimator.length; i++) { %>
            <tr>
              <td><%=estimator[i].ID %></td>
              <td><%=estimator[i].NAME %></td>
              <td><%=estimator[i].GENDER %></td>
              <td><%=estimator[i].ADDRESS %></td>
              <td><%=estimator[i].BIRTHDATE.toString().substring(4, 15) %></td>
              <td><%=estimator[i].PNUM %></td>
              <td><%=estimator[i].AGE %></td>
              <td><%=estimator[i].ROLE %></td>
            </tr>
            <% } %>
          </tbody>
        </table>
      </div>
      <hr class="mb-3" />
      <h3>검색 기능</h3>
      <form method="GET" action="/searchByRole">
        <div class="form-group">
          <label for="role"
            >역할 기준으로 검색 (제출자(식당) -> R , 제출자(보건소) -> H, 평가자
            -> E)</label
          >
          <input
            id="role"
            class="form-control"
            type="text"
            name="role"
            required
          />
        </div>
        <button class="btn btn-primary" type="submit" name="role">검색</button>
      </form>

      <form method="GET" action="/searchByAge">
        <div class="form-group">
          <label for="age"
            >나이대 기준으로 검색 ( (ex) 22살 -> 22, 30살 -> 30)</label
          >
          <input
            id="age"
            class="form-control"
            type="text"
            name="age"
            required
          />
        </div>
        <button class="btn btn-primary" type="submit" name="age">검색</button>
      </form>

      <form method="GET" action="/searchByGender">
        <div class="form-group">
          <label for="Gender">성별 기준으로 검색 ( 남자 -> M, 여자 -> F)</label>
          <input
            id="Gender"
            class="form-control"
            type="text"
            name="Gender"
            required
          />
        </div>
        <button class="btn btn-primary" type="submit" name="Gender">
          검색
        </button>
      </form>

      <form method="GET" action="/searchByTask">
        <div class="form-group">
          <label for="Task"
            >태스크 기준으로 검색 (참여중인 태스크 이름 입력)</label
          >
          <input
            id="Task"
            class="form-control"
            type="text"
            name="Task"
            required
          />
        </div>
        <button class="btn btn-primary" type="submit" name="Task">검색</button>
      </form>

      <form method="GET" action="/searchByID">
        <div class="form-group">
          <label for="ID">ID 기준으로 검색 (ID 입력)</label>
          <input id="ID" class="form-control" type="text" name="ID" required />
        </div>
        <button class="btn btn-primary" type="submit" name="ID">검색</button>
      </form>
    </div>
  </body>
</html>
