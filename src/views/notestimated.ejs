<!DOCTYPE html>
<html lang="ko">
  <head>
    <%- include('head'); -%>
    <title>To evaluate list(r)</title>
  </head>
  <body>
    <div class="container">
      <div class="row mt-4">
        <h2>평가 안된 태스크 목록(레스토랑)</h2>
        <hr class="mb-3" />
        <div class="table-responsive-sm"></div>
        <table class="table table-hover table-striped table-sm table-bordered">
          <thead class="thead-dark">
            <tr>
              <th>파일 번호</th>
              <th>제출자 아이디</th>
              <th>전체 튜플 수</th>
              <th>중복 튜플 수</th>
              <th>null 컬럼 비율</th>
            </tr>
          </thead>
          <tbody>
            <% for(i = 0; i < rparsed.length; i++) { %>
            <tr>
              <td><%=rparsed[i].PARSINGID %></td>
              <td><%=rparsed[i].SID %></td>
              <td><%=rparsed[i].TOTALTUPLE %></td>
              <td><%=rparsed[i].DUPTUPLE %></td>
              <td><%=rparsed[i].NULLRATE %></td>
            </tr>
            <% } %>
          </tbody>
        </table>
      </div>
      <hr class="mb-3" />
      <ul>
        <li><a href="/restimating">레스토랑 평가하러 가기</a></li>
        <li><a href="/estimatormain">평가된 레스토랑 목록</a></li>
        <li><a href="/estimatormain2">보건소 평가정보 보러가기</a></li>
        <li><a href="/"> 홈으로 돌아가기</a></li>
      </ul>
    </div>
  </body>
</html>
